# Kamal Secrets Configuration
# Copy this file to .kamal/secrets and configure your values
#
# These values are loaded from your .env file using shell command substitution
# Ensure your .env file contains the corresponding variables

# GitHub Container Registry password (Personal Access Token with write:packages scope)
# Generate at: https://github.com/settings/tokens/new?scopes=write:packages
KAMAL_REGISTRY_PASSWORD=$(cat .env | grep KAMAL_REGISTRY_PASSWORD | cut -d '=' -f2)

# LLM API Key (use "not-required" for local Ollama)
LLM_API_KEY=$(cat .env | grep LLM_API_KEY | cut -d '=' -f2)

# Deployment server configuration
DEPLOY_SERVER_IP=$(cat .env | grep DEPLOY_SERVER_IP | cut -d '=' -f2)
DEPLOY_DOMAIN=$(cat .env | grep DEPLOY_DOMAIN | cut -d '=' -f2)

# Authentication (Clerk) - optional
VITE_CLERK_PUBLISHABLE_KEY=$(cat .env | grep VITE_CLERK_PUBLISHABLE_KEY | cut -d '=' -f2)
AUTH_ENABLED=$(cat .env | grep AUTH_ENABLED | cut -d '=' -f2)
AUTH_ALLOW_SIGNUP=$(cat .env | grep AUTH_ALLOW_SIGNUP | cut -d '=' -f2)
